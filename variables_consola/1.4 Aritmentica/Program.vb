Imports System

Module Program
    Sub Main(args As String())
        Dim a As Integer = 5
        Dim b As Integer = 8
        Dim s As Single = 1.33
        Dim d As Decimal = 2.66
        Dim suma As Single
        Dim resta As Single
        Dim multiplicacion As Single
        Dim division As Single


        Console.WriteLine(" la suma de los enteros es: " & a + b)
        Console.WriteLine(" la resta de los enteros es: " & s - d)
        Console.WriteLine(" la multiplicacion de los enteros es: " & d * a)
        Console.WriteLine(" la divison de los enteros es: " & b / s)

    End Sub
End Module
